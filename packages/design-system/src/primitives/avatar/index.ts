/**
 * Avatar primitive — public API.
 *
 * NOTE: no runtime implementation of `<Avatar />` exists yet in
 * `apps/web/components/ui/`. These types seed the contract for the
 * future component; they can already be used to type props on custom
 * avatar UIs.
 */
export type { AvatarProps, AvatarSize, AvatarShape } from './avatar.types';
