/**
 * Card primitive — public API.
 *
 * Exposes the full Card composite (Card, CardHeader, CardTitle,
 * CardDescription, CardContent, CardFooter). Runtime implementations
 * live in `apps/web/components/ui/card.tsx`.
 */
export type {
  CardProps,
  CardHeaderProps,
  CardTitleProps,
  CardDescriptionProps,
  CardContentProps,
  CardFooterProps,
} from './card.types';
